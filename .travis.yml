language: python
sudo: false
dist: xenial
cache: pip

env:
  global:
    - PIP_WHEEL_DIR=$HOME/.cache/pip/wheels
    - PIP_FIND_LINKS=file://$HOME/.cache/pip/wheels

python:
  - "3.5"


script:
  - sphinx-build vdveldet/vdveldet.github.io/ -b html -a doc/_build/html

before_install:
  # - export CPPFLAGS=`gdal-config --cflags`
  # - export LDFLAGS=`gdal-config --libs`
  # - export PROJ_LIB=/usr/share/proj

install:
  - pip install -r doc-requirements.txt

# addons:
#   apt:
#     sources:
#     - sourceline: 'ppa:ubuntugis/ubuntugis-unstable'
#     packages:
#     - libgdal-dev
#     - gdal-bin
#     - pandoc  # for generating documentation from jupyter notebook

# after_success:
# - coveralls || echo "coveralls failure"

before_deploy:
  - pip install -r doc-requirements.txt
  - sphinx-build docs -a doc/_build/html

deploy:
    - user: vdvelde
      distributions: sdist bdist_wheel
      provider: pypi
      true:
        python: 3.5
        repo: vdveldet/vdveldet
        tags: true
    - provider: pages
      skip_cleanup: true
      github_token: $GH_TOKEN
      local_dir: doc/_build/html
      on:
        branch: master
        python: 3.5
